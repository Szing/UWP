<mvvm:MVVMPage x:ConnectionId='1' 
    x:Name="pageRoot"
    x:Class="NavDemo.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NavDemo"
    xmlns:mvvm="using:MVVMSidekick.Views"     
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:NavDemo.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
        <vm:AboutPage_Model x:Key="DesignVM"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"  DataContext="{StaticResource DesignVM}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <TextBlock x:ConnectionId='2' x:Name="pageTitle" Text="{Binding Title}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>

        <TextBox HorizontalAlignment="Left" Margin="480,165,0,0" Text="{Binding currentDialog.describeDialog}" TextWrapping="Wrap" VerticalAlignment="Top" Height="61" Width="358"/>
        <TextBox HorizontalAlignment="Left" Margin="480,93,0,0" Text="{Binding currentDialog.timeDialog}" TextWrapping="Wrap" VerticalAlignment="Top" Height="28" Width="129"/>

        <Button Content="LastPage" HorizontalAlignment="Left" Margin="138,785,0,0" VerticalAlignment="Top" Command="{Binding CommandToLastPage}"/>
        <Button Content="NextPage" HorizontalAlignment="Left" Margin="1180,785,0,0" VerticalAlignment="Top" Command="{Binding CommandToNextPage}"/>
        <RichEditBox x:ConnectionId='3' Name="editor" HorizontalAlignment="Left" Margin="431,252,0,0" VerticalAlignment="Top" Height="331" Width="528"/>
        <Button Content="show" HorizontalAlignment="Left" Margin="1014,281,0,0" VerticalAlignment="Top" Command="{Binding CommandGetDialog}" CommandParameter="{Binding ElementName=editor}" />


    </Grid>
</mvvm:MVVMPage>

